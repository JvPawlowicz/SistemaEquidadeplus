# EquidadePlus — variáveis para build e execução com Docker
# Copie para .env e preencha os valores.

# Supabase (obrigatório para build do frontend)
VITE_SUPABASE_URL=https://SEU_PROJECT_REF.supabase.co
VITE_SUPABASE_ANON_KEY=sua_anon_key_do_supabase

# Opcional: Cloudflare Tunnel (descomente o serviço cloudflared no docker-compose.yml)
# TUNNEL_TOKEN=seu_token_do_tunnel
